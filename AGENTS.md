# 📜 راهنمای توسعه و قوانین پروژه (HikStatus)

این سند راهنمای جامع توسعه، مدیریت نسخه و قوانین حاکم بر پروژه **HikStatus** برای ایجنت‌های هوشمند و توسعه‌دهندگان است.

---

## 📌 ۱. اصول عمومی، زبان و مستندسازی

* **زبان رسمی پروژه:** تمامی گفتگوها، نظرات درون کد (Inline Comments)، پیام‌های کامیت (Git Commits)، لاگ‌های سیستم و مستندات باید تماماً به **زبان فارسی روان** نوشته شوند.
* **فرمت تاریخ‌ها:** تمامی تاریخ‌ها در لاگ‌ها، مستندات و چنج‌لاگ باید به صورت **شمسی (YYYY/MM/DD)** ثبت شوند.
* **یکپارچگی طراحی و استایل (UI):** برای هرگونه ویرایش یا افزودن آیتم‌های رابط کاربری، رعایت دقیق مستندات و متغیرهای تعریف‌شده در `.agent/DESIGN.md` الزامی است. از تغییرات ad-hoc و خارج از سیستم طراحی خودداری کنید.

---

## 🔀 ۲. مدیریت شاخه‌ها و Git Workflow

* **توسعه روی شاخه اصلی (`main`):** توسعه مستقیم روی شاخه `main` انجام می‌شود. ایجاد Branch مجزا تنها برای آزمایش‌های بزرگ و پرریسک مجاز است.
* **پاکیزگی تاریخچه Git:** تمامی فایل‌های موقت، لاگ‌های تست، Scratchpadها و خروجی‌های موقت ایجنت‌ها باید در `.gitignore` قرار گیرند.

---

## 📦 ۳. استراتژی کامیت‌ها (Commit Strategy)

* **اعتبارسنجی و فرمت‌بندی اجباری قبل از کامیت (Pre-Commit Validation):** پیش از ثبت هر کامیت، ایجنت موظف است دستورات زیر را اجرا کرده و از سلامت ۱۰۰٪ آنها اطمینان حاصل کند:
  1. `uvx ruff format .` (فرمت‌بندی یکدست سورس‌کدها)
  2. `uvx ruff check --fix .` (بررسی و اصلاح خودکار هشدارهای لینت و مرتب‌سازی ایمپورت‌ها)
  3. اجرای موفق تست‌های محلی با `pytest`
* **تک‌کامیت به ازای هر Plan:** هر Implementation Plan، قابلیت جدید (Feature) یا رفع مشکل (Bugfix) باید پس از تکمیل و اعتبارسنجی کامل، در قالب **یک کامیت واحد** ثبت شود. از خرد کردن کار به کامیت‌های ریز و متوالی خودداری کنید.
* **شمول کامل تغییرات (Stage All Changes):** هنگام ثبت کامیت نهایی، تمامی تغییرات موجود در Workspace (کد، مستندات، قوانین و کانفیگ‌ها) باید بررسی شده و در همان تک‌کامیت شامل شوند (`git add -A` / `git commit`).
* **مدیریت تغییرات و کامیت مستندات (`.md`):** تغییرات مستندات، راهنماها و فایل‌های مارک‌داون نباید به صورت کامیت‌های جداگانه ثبت شوند. اگر تغییری صرفاً در مستندات یا فایل‌های توضیحی ایجاد شد، باید با استفاده از `git commit --amend` به کامیت قبلی اضافه (merge/amend) شود یا در همان کامیت اصلی کد قرار گیرد. همچنین تغییرات صرفاً مستنداتی نباید به عنوان یک نسخه یا رهاسازی جدید در `CHANGELOG` ثبت شوند.
* **پیام کامیت:** پیام‌ها باید کوتاه، شفاف، به زبان فارسی و شامل خلاصه تغییرات بخش‌های مختلف (کد، کانفیگ، مستندات) باشد.

---

## ⚙️ ۴. فرآیند حل خطاهای GitHub Actions

1. در صورت بروز خطای بیلد گیت‌هاب، خطا را با ابزارهای لازم (مانند GitHub CLI) بررسی و رفع کرده، بلافاصله کامیت و پوش کنید و منتظر اتمام پایپلاین بمانید.
2. اگر پس از یک‌بار تلاش مشکل برطرف نشد، از تکرار خودکار چرخه خودداری کرده، موضوع را به کاربر گزارش دهید و برای ادامه مسیر راهنمایی بخواهید.

---

## 🔄 ۵. به‌روزرسانی مستمر وضعیت پروژه (`.agents/PROJECT_STATE.md`)

ایجنت‌ها موظفند در جریان انجام تسک‌ها، در صورت وقوع هر یک از موارد زیر، بلافاصله بخش‌های مربوطه را در [`.agents/PROJECT_STATE.md`](file:///home/unreal/projects/HikStatus/.agents/PROJECT_STATE.md) به‌روزرسانی کنند:

1. **ثبت خطاها و بدهی‌های فنی برطرف‌نشده (`Known Issues & Tech Debt`):**
   - هرگونه خطای حل‌نشده، باگ شناسایی‌شده در حین کار، هشدارهای منسوخ‌شدگی (Deprecation) یا نیاز به ریفکتور که به تعویق می‌افتد، باید بلافاصله با توضیح شفاف در بخش ۵ این سند ثبت گردد.
2. **ثبت تصمیمات کلیدی اتخاذ‌شده (`Key Established Decisions`):**
   - هرگاه از کاربر سوالی پرسیده شود و کاربر تصمیم، سیاست یا مسیر مشخصی را تعیین کند، یا تصمیم معماری مهمی در پروژه نهایی شود، باید به صورت یک آیتم خلاصه و قاطع در بخش ۶ این سند ثبت گردد تا ایجنت‌های بعدی مجدداً همان سوالات را تکرار نکنند و از تصمیمات گذشته تبعیت کنند.