# مشکلات و باگ‌های پروژه HikStatus

## ⚠️ مشکلات باز (نیاز به حل)

### معماری و ساختار
- [ ] رمز های nvr ها بجای init_config.json در .env

---

## ✅ مشکلات حل شده

### باگ‌های تأیید شده
- [x] **عدم نمایش آیکون `is_recording` در UI** - ❌ حذف شد (این قابلیت دیگر استفاده نمی‌شد)
- [x] **تلگرام Proxy در Docker** - ❌ این یک باگ نیست. پروکسی برای روش‌های deployment مختلف قابل تنظیم است
- [x] **Admin session بعد از ری‌استارت از بین می‌رود** - توکن‌ها در دیتابیس ذخیره می‌شوند و Sliding Expiration ۳۰ روزه دارند

### مشکلات احتمالی حل شده
- [x] **ثبت نشدن تاریخ شمسی در لاگ‌ها** - با استفاده از jdatetime اصلاح شد
- [x] **عدم مدیریت timeout برای NVRهای قطع** - ✅ polling با `asyncio.gather` موازی اجرا می‌شود
- [x] **وابستگی داکر به شبکه خارجی vpn بدون مستندسازی** - ✅ مستندسازی کامل انجام شد

### مشکلات امنیتی حل شده
- [x] **SHA-256 بدون salt برای رمز عبور** - ✅ با PBKDF2-HMAC-SHA256 حل شد
- [x] **عدم رمزنگاری رمز عبور NVRها در دیتابیس** - ✅ با Fernet (AES) رمزنگاری می‌شود
- [x] **Admin session lifetime فقط 1 روز** - ✅ سشن‌ها ۳۰ روزه با Sliding Expiration شدند
- [x] **مقایسه مستقیم رمز عبور خام ادمین** - ✅ از پسوردهای هش‌شده با PBKDF2 پشتیبانی می‌شود
- [x] **فقدان محافظت در برابر CSRF** - ✅ با تطابق هدرهای Origin و Referer پیاده‌سازی شد
- [x] **آسیب‌پذیری در فرآیند بکاپ و بازیابی** - ✅ محدودیت حجم ۵۰ مگابایت و بررسی ساختار SQLite

### مشکلات UI حل شده
- [x] **عدم نمایش خطا در آپلoad نقشه** - ✅ خطاها با showToast فارسی نمایش داده می‌شوند
- [x] **عدم Responsive جداول در موبایل** - ✅ اسکرول افقی و بهینه‌سازی فونت فعال شد
- [x] **هشدار قطع اتصال WebSocket فقط انگلیسی** - ✅ پیام‌ها فارسی شده‌اند
- [x] **محدودیت تک‌نقشه‌ای فرانت‌اند** - ✅ پشتیبانی از چند نقشه مجزا پیاده‌سازی شد
- [x] **UI ضعیف وظایف زمان‌بندی شده** - ✅ بازطراحی با کارت‌های responsive، فرمت زمان فارسی، تایید قبل از اجرا و نمایش خطا

---

## ℹ️ مشکلات غیر مهم (نیاز به حل ندارند)

این مشکلات در حال حاضر برای پروژه بحرانی نیستند و فقط برای آگاهی ثبت شده‌اند.

### معماری و ساختار
- **استفاده از وضعیت‌های سراسری تغییرپذیر (Global Mutable State)** - متغیرهایی مانند `_login_attempts` و `_config_cache` در کد وجود دارند. برای اپلیکیشن تک‌پروسه‌ای فعلی با asyncio مشکلی ایجاد نمی‌کنند اما در صورت اضافه شدن multiprocessing ممکن است نیاز به بازبینی داشته باشند.

### کیفیت کد
- **مدیریت خطاهای بسیار کلی (Broad Exception Handling)** - ۲ مورد `except:` و ۳۰ مورد `except Exception` در کد وجود دارد. بررسی نشان داد این استثناها در سناریوهای مناسبی استفاده شده‌اند (حذف فایل‌های قدیمی، حذف اتصالات مرده WebSocket) که نوع خطا مهم نیست و عملیات غیربحرانی است. نیاز به تغییر ندارد.
