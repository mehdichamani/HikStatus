# مستند قابلیت‌های قابل استخراج از NVR

بر اساس مستندات پروتکل ISAPI، می‌توان اطلاعات و وضعیت‌های حیاتی سیستم را از طریق وب‌سرویس NVR دریافت کرد. موارد زیر استخراج شده‌اند:

## قابلیت‌های پیاده‌سازی شده در HikStatus:

1. **مشخصات فیزیکی دستگاه (Device Info)**
   * **کاربرد:** استخراج مشخصاتی مانند نام مدل NVR، نسخه Firmware نصب شده، آدرس فیزیکی شبکه (MAC Address) و شماره سریال.
   * **مسیر ISAPI مورد استفاده:** `/ISAPI/System/deviceInfo`

2. **وضعیت منابع سخت‌افزاری (System Status)**
   * **کاربرد:** نظارت روی سلامت پردازنده مرکزی (CPU Usage)، حافظه موقت (Memory Usage) و همچنین مدت زمانی که دستگاه بدون خاموشی روشن بوده است (Uptime).
   * **مسیر ISAPI مورد استفاده:** `/ISAPI/System/status`

3. **وضعیت هارد دیسک‌ها (Storage / HDD Status)**
   * **کاربرد:** مانیتورینگ سلامت هاردها، ظرفیت کل و فضای ذخیره‌سازی خالی. در صورت خرابی یا فرمت نشدن هارد دیسک، می‌توان وضعیت آن (مثلا Error یا Unformatted) را به سرعت شناسایی کرد.
   * **مسیر ISAPI مورد استفاده:** `/ISAPI/ContentMgmt/Storage`

4. **زمان داخلی دستگاه (Device Time)**
   * **کاربرد:** بررسی ساعت و تاریخ داخلی NVR. تنظیم نبودن ساعت NVR می‌تواند مشکلاتی را در زمینه Playback یا دریافت لاگ‌ها به وجود آورد.
   * **مسیر ISAPI مورد استفاده:** `/ISAPI/System/time`


## قابلیت‌های پیشنهادی برای توسعه‌های آینده:
موارد زیر نیز قابل دریافت هستند اما در نسخه فعلی پیاده‌سازی نشده‌اند:
* **نظارت بر شبکه:** دریافت پورت‌های شبکه فعال، پهنای باند و MTU از طریق `/ISAPI/System/Network/interfaces`.
* **لاگ‌های دستگاه:** دریافت لاگ‌های ورود ناموفق یا خاموش شدن‌های ناگهانی به صورت دوره‌ای از طریق `/ISAPI/ContentMgmt/logSearch`.
* **مانیتورینگ استریم لحظه‌ای (Bitrate & Framerate):** دریافت اطلاعات کیفیت استریم برای اطمینان از سلامت کابل‌ها و پهنای باند کافی برای هر دوربین.
